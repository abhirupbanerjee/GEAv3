{
  "$comment": "Registry of available AI agents. To add a new agent, append an entry below. NEVER put bearer tokens in this file. Tokens are read from the environment variable named in `tokenEnv` (server-side only).",
  "agents": []
}
