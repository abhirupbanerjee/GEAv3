/**
 * Hook for accessing ChatContext
 * Re-exports the hook from ChatContextProvider for centralized import location
 */

export { useChatContext, useChatContextOptional } from '@/providers/ChatContextProvider';
