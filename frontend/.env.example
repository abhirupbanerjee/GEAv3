# ============================================================================
# GEA PORTAL - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env.local and update with your actual values
# IMPORTANT: Never commit .env.local to version control
# ============================================================================

# ----------------------------------------------------------------------------
# NEXTAUTH CONFIGURATION
# ----------------------------------------------------------------------------

# NextAuth URL - Your application URL
# Production: https://gea.abhirup.app
# Development: http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret - Used to encrypt JWT tokens
# Generate with: openssl rand -base64 32
# IMPORTANT: Keep this secret and never share it
NEXTAUTH_SECRET=

# ----------------------------------------------------------------------------
# GOOGLE OAUTH CONFIGURATION
# ----------------------------------------------------------------------------
# Setup: https://console.cloud.google.com/
# 1. Create a new project (or use existing)
# 2. Enable Google+ API
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 5. For production, also add: https://gea.abhirup.app/api/auth/callback/google

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ----------------------------------------------------------------------------
# MICROSOFT OAUTH CONFIGURATION (Optional)
# ----------------------------------------------------------------------------
# Setup: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
# 1. Register new application
# 2. Add redirect URI: http://localhost:3000/api/auth/callback/microsoft-entra-id
# 3. For production, also add: https://gea.abhirup.app/api/auth/callback/microsoft-entra-id
# 4. Create client secret (valid for 24 months recommended)
# 5. Note the Application (client) ID, Tenant ID, and client secret

# MICROSOFT_CLIENT_ID=
# MICROSOFT_CLIENT_SECRET=
# MICROSOFT_TENANT_ID=common

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ----------------------------------------------------------------------------
# PostgreSQL connection details
# These should match your docker-compose.yml settings

DB_HOST=localhost
DB_PORT=5432
DB_NAME=feedback
DB_USER=feedback_user
DB_PASSWORD=your_secure_password_here

# ----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# ----------------------------------------------------------------------------

# Node environment
NODE_ENV=development

# API URL (if different from NEXTAUTH_URL)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api

# ----------------------------------------------------------------------------
# OPTIONAL: LOGGING & MONITORING
# ----------------------------------------------------------------------------

# Enable NextAuth debug logging (set to true in development)
# NEXTAUTH_DEBUG=true

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Copy this file:
#    cp .env.example .env.local
#
# 2. Generate NEXTAUTH_SECRET:
#    openssl rand -base64 32
#
# 3. Set up Google OAuth:
#    - Go to https://console.cloud.google.com/
#    - Follow the instructions in the GOOGLE OAUTH section above
#    - Copy Client ID and Client Secret to this file
#
# 4. (Optional) Set up Microsoft OAuth:
#    - Go to https://portal.azure.com/
#    - Follow the instructions in the MICROSOFT OAUTH section above
#    - Uncomment and fill in the MICROSOFT_* variables
#
# 5. Update database credentials:
#    - Ensure DB_PASSWORD matches your docker-compose.yml
#    - For production, use a strong password
#
# 6. Add your first admin user:
#    cd ..
#    ADMIN_EMAIL=your@email.com ADMIN_NAME="Your Name" ./database/05-add-initial-admin.sh
#
# 7. Start the development server:
#    npm run dev
#
# 8. Test authentication:
#    - Visit http://localhost:3000/admin
#    - Click "Sign in with Google"
#    - Sign in with your admin email
#
# ============================================================================
